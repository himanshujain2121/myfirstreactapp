import React from "react";
import styles from "./Navbar.module.css";

const Logo = () => {
    return (
        <div className={styles.logo}>
            meesho
        </div>
    );
};

export default Logo;